[package]
name = "titan_engine"
version = "1.0.0"
edition = "2021"
authors = ["Julius Cameron Hill"]

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
sysinfo = "0.30"

[build-dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
